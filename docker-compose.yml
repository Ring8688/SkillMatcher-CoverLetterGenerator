version: '3.8'

services:
  app:
    build: .
    container_name: skill-matcher-app
    ports:
      - "8501:8501"
    volumes:
      - ./config.yml:/app/config.yml:ro
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
